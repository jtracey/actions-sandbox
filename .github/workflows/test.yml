name: test
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: cat /etc/passwd
      - run: sudo sed -i 's/:,/:runner name,/' /etc/passwd
      - run: cat /etc/passwd
      - run: ls -l /var/run/utmp
      - run: od -c /var/run/utmp
      - run: pinky
      - run: whoami
      - run: echo "$USER"
      - run: pinky -l "$USER"
      - run: pinky -l runneradmin

